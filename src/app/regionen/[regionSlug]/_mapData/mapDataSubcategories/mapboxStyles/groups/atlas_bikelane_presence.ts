// Autogenerated by `scripts/MapboxStyles/process.ts`
// Do not change this file manually

import { MapboxStyleLayer } from '../types'

export const mapboxStyleGroupLayers_atlas_bikelane_presence: MapboxStyleLayer[] = [
  {
    filter: ['has', 'bikelane_right'],
    type: 'line',
    id: 'bikelane_right',
    paint: {
      'line-color': [
        'case',
        ['match', ['get', 'bikelane_right'], ['missing'], true, false],
        '#fa80f4',
        ['match', ['get', 'bikelane_right'], ['not_expected'], true, false],
        'rgba(234, 200, 210, 0.68)',
        ['match', ['get', 'bikelane_right'], ['data_no'], true, false],
        'rgba(108, 124, 147, 0.67)',
        ['has', 'bikelane_right'],
        'rgba(142, 177, 240, 0.7)',
        'rgba(0, 0, 0, 0.7)',
      ],
      'line-offset': ['step', ['zoom'], 1, 14, 4],
      'line-width': [
        'interpolate',
        ['linear'],
        ['zoom'],
        8,
        1,
        16,
        ['match', ['get', 'bikelane_right'], ['missing'], 8, 4],
      ],
      'line-opacity': [
        'interpolate',
        ['linear'],
        ['zoom'],
        10,
        ['match', ['get', 'bikelane_right'], ['missing'], 1, 0],
        11,
        1,
      ],
    },
  },
  {
    filter: ['has', 'bikelane_self'],
    type: 'line',
    id: 'bikelane_self',
    paint: {
      'line-color': [
        'case',
        ['match', ['get', 'bikelane_self'], ['missing'], true, false],
        '#fa80f4',
        ['match', ['get', 'bikelane_self'], ['not_expected'], true, false],
        'rgba(234, 200, 210, 0.68)',
        ['match', ['get', 'bikelane_self'], ['data_no'], true, false],
        'rgba(108, 124, 147, 0.67)',
        ['has', 'bikelane_self'],
        'rgba(142, 177, 240, 0.7)',
        'rgba(0, 0, 0, 0.7)',
      ],
      'line-width': [
        'interpolate',
        ['linear'],
        ['zoom'],
        8,
        1,
        16,
        ['match', ['get', 'bikelane_self'], ['missing'], 8, 4],
      ],
      'line-opacity': [
        'interpolate',
        ['linear'],
        ['zoom'],
        10,
        ['match', ['get', 'bikelane_self'], ['missing'], 1, 0],
        11,
        1,
      ],
    },
  },
  {
    filter: ['has', 'bikelane_left'],
    type: 'line',
    id: 'bikelane_left',
    paint: {
      'line-color': [
        'case',
        ['match', ['get', 'bikelane_left'], ['missing'], true, false],
        '#fa80f4',
        ['match', ['get', 'bikelane_left'], ['not_expected'], true, false],
        'rgba(234, 200, 210, 0.68)',
        ['match', ['get', 'bikelane_left'], ['data_no'], true, false],
        'rgba(108, 124, 147, 0.67)',
        ['has', 'bikelane_left'],
        'rgba(142, 177, 240, 0.7)',
        'rgba(0, 0, 0, 0.7)',
      ],
      'line-offset': ['step', ['zoom'], -1, 14, -4],
      'line-width': [
        'interpolate',
        ['linear'],
        ['zoom'],
        8,
        1,
        16,
        ['match', ['get', 'bikelane_left'], ['missing'], 8, 4],
      ],
      'line-opacity': [
        'interpolate',
        ['linear'],
        ['zoom'],
        10,
        ['match', ['get', 'bikelane_left'], ['missing'], 1, 0],
        11,
        1,
      ],
    },
  },
]
