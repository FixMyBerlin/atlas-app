# this file is copied to the staging or production server to be used there

version: "3"
services:
  app:
    env_file:
      - ./.env
    image: public.ecr.aws/n0p8j4k5/atlas/blitz:${GITHUB_SHA}
    container_name: blitz
    environment:
      SESSIONS_SECRET_KEY: ${SESSION_SECRET_KEY}
      DATABASE_URL: ${DATABASE_URL}
    network_mode: host
    ports:
      - 3000:3000
    labels:
      - "traefik.enable=true"
      - "traefik.http.services.staging.loadbalancer.server.port=3000"
      - "traefik.http.routers.staging.rule=Host(`staging.radverkehrsatlas.de`)"
      - "traefik.http.routers.staging.entrypoints=websecure"
      - "traefik.http.routers.staging.tls.certresolver=letsencrypt"
      - "traefik.http.routers.staging.tls=true"
