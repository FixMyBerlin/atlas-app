name: Run Tests
on: [push, pull_request]
jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Build Image
        run: |
          docker compose build processing
      - name: Run Tests
        run: |
          ./run-tests.sh
