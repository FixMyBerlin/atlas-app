require("Metadata")

function IntoExcludeTable(table, object, reason)
  -- table:insert({
  --   tags = object.tags,
  --   meta = Metadata(object),
  --   reason = reason,
  --   geom = object:as_linestring()
  -- })
end
