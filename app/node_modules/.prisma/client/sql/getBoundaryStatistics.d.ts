import * as $runtime from "@prisma/client/runtime/library"

/**
 * @param text
 */
export const getBoundaryStatistics: (text: string) => $runtime.TypedSql<getBoundaryStatistics.Parameters, getBoundaryStatistics.Result>

export namespace getBoundaryStatistics {
  export type Parameters = [text: string]
  export type Result = {
    name: string | null
    road_length: $runtime.JsonValue | null
    bikelane_length: $runtime.JsonValue | null
  }
}
